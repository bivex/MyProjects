# 📅 {{date:dddd, DD MMMM YYYY}}

## 🎯 Задачи на сегодня

### 🔥 Важные и срочные
```dataview
TABLE WITHOUT ID
  file.link AS "Задача",
  choice(priority = "high", "🔴", "🟡") AS "!",
  timeEstimate AS "Время",
  projects AS "Проект"
FROM #task
WHERE (due AND date(due).date() == date("{{date}}")) OR (scheduled AND date(scheduled).date() == date("{{date}}"))
WHERE priority = "high" OR (due AND date(due).date() == date("{{date}}"))
SORT priority DESC, due ASC
```

### 📋 Обычные задачи
```dataview
TABLE WITHOUT ID
  file.link AS "Задача",
  priority AS "Приоритет",
  timeEstimate AS "Время",
  projects AS "Проект"
FROM #task
WHERE (due AND date(due).date() == date("{{date}}")) OR (scheduled AND date(scheduled).date() == date("{{date}}"))
WHERE priority != "high" AND NOT (due AND date(due).date() == date("{{date}}") AND priority = "high")
SORT priority DESC, due ASC
```

## 📊 Прогресс вчера

```dataview
TABLE WITHOUT ID
  "Выполненные задачи" AS "Показатель",
  length(rows.where(status = "done" AND completed AND date(completed) == date("{{date}}") - dur(1 day))) AS "Значение"
FROM #task
```

## 🚨 Просроченные задачи

```dataview
LIST
FROM #task
WHERE due AND date(due) < date("{{date}}") AND status != "done"
SORT due ASC
```

## 📅 План на завтра

```dataview
TABLE WITHOUT ID
  file.link AS "Задача",
  priority AS "Приоритет",
  projects AS "Проект"
FROM #task
WHERE (due AND date(due).date() == date("{{date}}") + dur(1 day)) OR (scheduled AND date(scheduled).date() == date("{{date}}") + dur(1 day))
SORT priority DESC
```

## 📝 Заметки и идеи

- [ ] Идея 1
- [ ] Идея 2
- [ ] Важная заметка

## 🎯 Рефлексия дня

**Что удалось хорошо:**
-

**Что можно улучшить:**
-

**Уроки на завтра:**
-

## 📈 Метрики дня

- ⏱️ Время работы: ____ часов
- ✅ Выполнено задач: ____
- 🎯 Цели достигнуты: ____%
- 📚 Новых знаний: ____

## 🔗 Связи

- [[{{yesterday}}|Вчерашний день]]
- [[{{tomorrow}}|Завтрашний день]]
- [[Еженедельный план#{{date:gggg-[W]ww}}|Недельный план]]
